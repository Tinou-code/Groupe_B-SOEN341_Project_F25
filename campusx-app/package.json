{
  "name": "campusx-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "startlocal": "npm run dev --prefix backend",
    "lint": "eslint .",
    "preview": "vite preview"
    
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
